Algorithme Ex11
Var
    T : Tableau[100] de réel
    i,N,nbAdmis : Entier
Début
	    //Remplir le tableau : utiliser la boucle 
	    T = [16,8,10,13,19,17,9,5,16,1]
	    N = 10
	    //compter le nombre des Admis ( >=10)
	    nbAdmis = 0
	    pour i de 0 à N-1 Faire
	    	        Si T[i] >= 10 Alors
	    	            nbAdmis = nbAdmis + 1
	    	        FinSi
	    finpour
	    Ecrire("Le nombre des Admis est ",nbAdmis)
Fin
